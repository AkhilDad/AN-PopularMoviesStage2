<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="com.udacity.popularmoviesstage2.viewmodel.MovieReviewVM" />

        <variable
            name="reviewVM"
            type="MovieReviewVM" />
    </data>

    <LinearLayout
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">

        <android.support.v7.widget.AppCompatTextView
            android:id="@+id/tv_reviewer_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:gravity="center"
            android:background="@color/colorPrimaryDark"
            android:maxLines="2"
            android:text="@{reviewVM.reviewerName}" />

        <android.support.v7.widget.AppCompatTextView
            android:id="@+id/tv_reviewer_comment"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:gravity="center"
            android:text="@{reviewVM.reviewComment}"
            android:onClick="@{reviewVM.onViewReview}"/>
    </LinearLayout>

</layout>